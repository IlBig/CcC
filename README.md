# CcC

Stack-agnostic coding workflow built around Claude Code as the primary implementer and Codex CLI as the independent reviewer.

Use `ccc MyProject` to bootstrap a new project with templates, Claude skills, the pre-commit Codex review hook, and the PreCompact reminder hook.

Run the repository checks with:

```bash
./tests/run.sh
```
